<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="5dp"
    android:descendantFocusability="blocksDescendants"
    android:orientation="horizontal">

    <Button
        android:layout_width="50dp"
        android:layout_height="75dp"
        android:id="@+id/btnBookCover"
        android:background="@drawable/one"
        android:layout_weight="1"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:id="@+id/store_list_item_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:ellipsize="end"
        android:lines="1"
        android:padding="5dip"
        android:scrollHorizontally="true"

        android:textColor="@android:color/holo_green_dark"
        android:textSize="30sp"
        android:textStyle="normal"
        android:text="Title"
        android:layout_weight="3.70"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/btnBookCover"
        android:layout_toEndOf="@+id/btnBookCover">
    </TextView>

    <TextView
        android:id="@+id/textViewDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:ellipsize="end"
        android:gravity="center"
        android:lines="1"
        android:padding="5dip"
        android:scrollHorizontally="true"
        android:textColor="@android:color/holo_green_dark"
        android:textSize="20sp"
        android:textStyle="normal"
        android:text="Description of the book about the subject and nture of the book."
        android:layout_weight="3.70"
        android:layout_below="@+id/store_list_item_text"
        android:layout_toRightOf="@+id/btnBookCover"
        android:layout_toEndOf="@+id/btnBookCover" />

</RelativeLayout>